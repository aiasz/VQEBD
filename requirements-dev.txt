# =============================================================================
# VQEBD — fejlesztői és tesztelési függőségek
# =============================================================================
# Minden verzió rögzített (==), mert a minőségi kapuk eredménye a fázisok
# elfogadási kritériuma: egy lint-szabály csendes megváltozása nem tehet
# egy korábban lezárt fázist utólag pirossá.
# Ellenőrizve a PyPI-n: 2026-09-22.
# =============================================================================

pytest==9.1.1          # tesztfuttató
pytest-cov==7.1.0      # lefedettség (Fázis 1-től használjuk érdemben)
ruff==0.16.8           # lint + formázás (a flake8/black/isort helyett, egy eszközben)
mypy==2.3.1            # statikus típusellenőrzés, strict módban a src/vqebd felett

